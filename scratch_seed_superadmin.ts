import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); import bcrypt from "bcryptjs"; async function main() { const passwordHash = await bcrypt.hash("password123", 10); await prisma.user.upsert({ where: { email: "superadmin@dexze.com" }, update: { passwordHash, role: "SUPER_ADMIN" }, create: { email: "superadmin@dexze.com", passwordHash, role: "SUPER_ADMIN" } }); console.log("Super admin seeded: superadmin@dexze.com / password123"); } main().catch(console.error).finally(() => prisma.$disconnect());
